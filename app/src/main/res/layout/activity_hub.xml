<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/ivHubBg"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:src="@drawable/bg_party_castle" />

    <View
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#A6000000" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView
            android:id="@+id/tvTime"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            style="@style/GrimTextHeader"
            android:gravity="center"
            android:layout_marginBottom="16dp" />

        <!-- CORE NAVIGATION -->
        <GridLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:columnCount="3"
            android:alignmentMode="alignMargins">

            <Button android:id="@+id/openCity" style="@style/GrimNavButton" android:text="Wjedź do Miasta" />
            <Button android:id="@+id/openMap" style="@style/GrimNavButton" android:text="Atlas Świata" />
            <Button android:id="@+id/openInventory" style="@style/GrimNavButton" android:text="Ekwipunek" />

            <Button android:id="@+id/openQuests" style="@style/GrimNavButton" android:text="Zadania" />
            <Button android:id="@+id/openCombatStatus" style="@style/GrimNavButton" android:text="Walka" />
            <Button android:id="@+id/openReputation" style="@style/GrimNavButton" android:text="Reputacja" />

        </GridLayout>

        <!-- PARTY STRIP VISIBILITY -->
        <include layout="@layout/strip_party" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:layout_marginTop="8dp">
            <Button android:id="@+id/btnSave" style="@style/GrimSmallButton" android:text="Zapisz" android:layout_width="100dp" />
            <Button android:id="@+id/btnLoad" style="@style/GrimSmallButton" android:text="Wczytaj" android:layout_width="100dp" />
            <Button android:id="@+id/openFinale" style="@style/GrimSmallButton" android:text="FINAŁ" android:layout_width="100dp" />
        </LinearLayout>

    </LinearLayout>
</FrameLayout>
