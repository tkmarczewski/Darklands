<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:name=".GrimReichApp"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@style/Theme.GrimReich"
        android:supportsRtl="true">

        <!-- INITIAL LAUNCHER: Splash -> Main Menu -->
        <activity android:name=".ui.SplashActivity"
            android:exported="true"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".ui.MainMenuActivity"
            android:exported="false"
            android:screenOrientation="landscape">
        </activity>

        <activity android:name=".ui.PlayerIdentityActivity"
            android:exported="false"
            android:screenOrientation="landscape">
        </activity>

        <!-- SINGLE ACTIVITY ROOT: ALL GAMEPLAY HAPPENS HERE -->
        <activity android:name=".ui.MainActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:screenOrientation="landscape">
        </activity>

        <activity android:name=".ui.CharacterCreatorActivity"
            android:exported="false"
            android:screenOrientation="landscape"/>

        <activity android:name=".ui.DevMenuActivity"
            android:exported="true"
            android:screenOrientation="landscape"/>

    </application>
</manifest>
